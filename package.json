{
  "name": "GYM Portfolio",
  "version": "1.0.0",
  "description": "GYM Portfolio reponsive webite build using HTML5, CSS3 and Javascript. The complete layout of the page is build using grid layout and flexbox with some cool animations.",
  "main": "index.html",
  "scripts": {
    "watch:sass": "node-sass sass/main.scss css/style.css -w",
    "prefix:css": "postcss --use autoprefixer -b 'last 5 versions' css/style.css -o css/style.css",
    "parcel": "parcel build index.html -d dist",
    "build": "npm run prefix:css && npm run parcel",
    "dev": "parcel index.html"
  },
  "author": "Rohit Kumar",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "node-sass": "^9.0.0",
    "parcel": "^2.10.3",
    "parcel-bundler": "^1.12.5",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0"
  },
  "dependencies": {
    "aos": "^3.0.0-beta.6",
    "leaflet": "^1.9.4"
  }
}
